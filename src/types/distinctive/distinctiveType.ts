import { StaticImageData } from "next/image";

export type DistinctiveType = {
  className?: string;
  video: string;
  service: string;
  title: string;
  desc: string;
  icon : StaticImageData
};
